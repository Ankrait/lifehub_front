import { Box, Typography } from '@mui/material';
import { FC } from 'react';

const Main: FC = () => {
	return <Box>
		<Typography variant='h4'>
			Главная
		</Typography>
	</Box>;
};

export default Main;
